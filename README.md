# an0mas-discord-bot-v2

TypeScript + Sapphire Framework（discord.js v14）で作る Discord Bot です。

## 起動手順
- `npm i`
- `npm run dev`

## コマンド登録
- `npm run deploy`

## 環境変数
- `.env.example` をコピーして `.env` を作成
- 必須：`DISCORD_TOKEN`, `CLIENT_ID`, `GUILD_ID`

## DB準備
- `./data/dev.sqlite3` を利用（起動時に自動生成）
